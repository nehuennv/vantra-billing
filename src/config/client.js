export const clientConfig = {
    name: "VantraBill",
    // logo: "/path/to/logo.png", // Optional: if present, overrides name
    colors: {
        primary: "#059669", // Indigo-600 (Vibrant & Deep)

        // Slavic/Blue-ish dark (HSL)
        // You can add more brand colors here if needed
    }
};
